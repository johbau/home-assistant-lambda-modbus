{
  "config": {
    "title": "Lambda Modbus",
    "step": {
      "user": {
        "title": "Define your Lambda modbus connection",
        "data": {
          "host": "The ip-address of your Lambda device",
          "name": "The prefix to be used for your Lambda sensors",
          "port": "The TCP port on which to connect to the Lambda",
          "modbus_address": "The modbus address",
          "energy_manager": "Enable setting active energy manager",
          "read_heat_pump_1": "Read heat pump 1 data (only when equipped)",
          "read_heat_pump_2": "Read heat pump 2 data (only when equipped)",
          "read_heat_pump_3": "Read heat pump 3 data (only when equipped)",
          "read_boiler_1": "Read boiler 1 data (only when equipped)",
          "read_boiler_2": "Read boiler 2 data (only when equipped)",
          "read_boiler_3": "Read boiler 3 data (only when equipped)",
          "read_boiler_4": "Read boiler 4 data (only when equipped)",
          "read_boiler_5": "Read boiler 5 data (only when equipped)",
          "read_buffer_1": "Read buffer 1 data (only when equipped)",
          "read_buffer_2": "Read buffer 2 data (only when equipped)",
          "read_buffer_3": "Read buffer 3 data (only when equipped)",
          "read_buffer_4": "Read buffer 4 data (only when equipped)",
          "read_buffer_5": "Read buffer 5 data (only when equipped)",
          "scan_interval": "The modbus registers polling interval [s]"
        }
      }
    },
    "error": {
      "already_configured": "Device is already configured"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}
